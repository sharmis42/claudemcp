services:
  - type: web
    name: figma-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node --no-deprecation dist/index.js
    healthCheckPath: /health
    envVars:
      - key: FIGMA_ACCESS_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
